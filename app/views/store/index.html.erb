<div class="store">
  <%= render 'layouts/masthead' %>
  
  <section class="products">
  <% @products.each do |p| %>
    <div class="col-md-3">
      <div class="product" data-toggle="modal" data-target="#id">
        <img alt="img" src="<%=p.image_1%>" />
        <p class="name"><%=p.name%></p>
        <p class="price"> <%=@merchant.currency_code%> <%=p.price%></p>
      </div>    
    </div>    
    <%= render :partial => 'products/product_popup', :object => p %>
    <% end %>    
    
  </section>

  
</div>

